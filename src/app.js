/**
 * Welcome to Pebble.js!
 *
 * This is where you write your app.
 */

var UI = require('ui');
// Initialize a card face
var card = new UI.Card({title:'Weather',subtitle:'Fetching...'});
// Display the card
card.show();
